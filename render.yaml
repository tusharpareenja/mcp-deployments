services:
  - type: web
    name: todo-list-mcp-mcp
    runtime: python311
    buildCommand: pip install -r requirements.txt
    startCommand: python main.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.11
    autoDeploy: true
